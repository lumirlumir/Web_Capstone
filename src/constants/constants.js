const PUBLIC_URL = `${process.env.PUBLIC_URL}`;

const URL = {
  public: PUBLIC_URL,
  start: `${PUBLIC_URL}/`,
  interview: `${PUBLIC_URL}/interview`,
  result: `${PUBLIC_URL}/result`,
};

export default URL;
