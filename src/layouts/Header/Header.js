import React from 'react';

import './Header.scss'

/**
 *
 * @returns Header
 */
const Header = () => {
  return (
    <header>
      <strong>header</strong>
    </header>
  );
}

export default Header;
